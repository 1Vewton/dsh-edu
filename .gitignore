node_modules/
.pnpm-store/
*.tgz
*.log

# lib/ is intentionally committed: it is what the loader imports, so a clone
# (or `dsh plugin add ./` and `dsh plugin add github:...`) works without
# granting a build script permission. It is regenerated by `pnpm build`.
